[tool.ruff.lint.flake8-bugbear]
# FastAPI's dependency-injection pattern requires calling Depends() (and
# similar) as a default argument value -- that's how FastAPI discovers
# dependencies from a function's signature. Without this, Ruff's B008 rule
# flags the framework's own required pattern as a bug.
extend-immutable-calls = ["fastapi.Depends"]
